/**
 * 1. encapsulate.rs     use public/private scope to encapsulate
 * 2. polymorphism.rs    use trait to implement
 * 3. state_pattern.rs   use state sample to illustrate the oop programming in Rust.
 * 4. state_pattern2.rs  optimize
 */
pub mod encasulation;
pub mod polymorphism;
pub mod state_pattern;
pub mod state_pattern2;
