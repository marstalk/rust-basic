use crate::algorithm::tree::binary_tree::{BTree, Node};


/**
 * p and q must in the binary search tree.
 */
pub fn lowest_common_ancestor_bst(btree: &BTree, p: i32, q: i32) {}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::algorithm::tree::binary_tree::BTree;

}
