/**
 * if nums are sorted, then the space complexity is O(1). by remembering the last element.
 *
 * For double pointer solution,
 * 1. what is slow mean? is important.
 *
 * if nums are not sorted, then the space complexity is O(n).
 */
pub mod move_zero;
pub mod remove_duplicate_in_sorted_array;
pub mod remove_duplicate_in_sorted_array2;
pub mod remove_element;
